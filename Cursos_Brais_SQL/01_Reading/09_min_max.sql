--MIN MAX

/*Con estos comandos lo ideal es sacar solo el dato que queramos buscar, ya sea el máximo
o el mínimo, si queremos filtrar con más campos, hay que combinar la consulta de otra
manera, se verá más adelante.*/


SELECT MAX(age) FROM users;

SELECT MIN(age) FROM users;